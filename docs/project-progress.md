# 项目进度台账（Dungeon Courier）

最后更新：2026-03-05

## 已实现（Done）
- [x] 项目基础结构拆分：`game/mod/data/map/path/combat`
- [x] 命令行参数：`--seed`、`--width`、`--height`
- [x] 随机地牢生成（房间 + L 走廊）
- [x] 同 seed 地图可复现
- [x] 主线闭环：拾取包裹 `P` -> 到达出口 `E` 胜利
- [x] 玩家基础回合行为：移动/近战/拾取/喝药/等待/退出
- [x] 怪物基础 AI：视线追踪 + BFS 下一步 + 随机巡走
- [x] 战斗公式：`max(1, atk - def + rng(-1..=1))`
- [x] FOV + 迷雾记忆（未探索隐藏、探索后保留）
- [x] 数据驱动基础：`assets/items.json`、`assets/monsters.json`
- [x] 基础测试：连通性、寻路、伤害边界
- [x] `ratatui` 自适应分栏 UI（地图/状态/任务/日志）
- [x] 弹窗交互：`i` 背包、`?` 帮助、`Esc` 关闭弹窗或退出
- [x] UI 模式切换与布局比例单测（新增 2 项）
- [x] 终端尺寸约束修复：启动时按可视区自动收缩地图，避免小终端截断
- [x] 包裹交互修复：走到 `P` 上自动拾取（不再依赖 `g`）
- [x] 输入事件修复：仅响应 `KeyEventKind::Press`，避免一次按键移动两格
- [x] 存档/读档：`F2` 保存、`F3` 读取（`saves/save1.json`）
- [x] 存档回归测试：键位映射与核心状态 roundtrip
- [x] 背包交互升级：`w/s` 选择条目，`Enter` 使用，`x` 丢弃
- [x] 任务包裹保护：包裹条目不可使用、不可丢弃
- [x] 背包回归测试：选择不移动角色、使用/丢弃回合结算、包裹只读
- [x] 背包迁移为通用物品栈（不再硬编码 `potions/has_package` 字段）
- [x] 丢弃行为落地：背包丢弃会在玩家脚下生成地面物品
- [x] 存档结构随背包迁移（旧存档不兼容）
- [x] 装备系统 MVP：武器/护甲/饰品 3 槽位（`Enter` 装备，`r` 卸下）
- [x] 装备数值生效：ATK/DEF 使用最终值（基础值 + 装备加成）
- [x] 装备交互保护：已装备物品不可直接丢弃（需先卸下）
- [x] UI 同步：背包条目标记“已装备”，帮助弹窗新增 `r` 说明
- [x] AI 状态机增强：`Patrol / Alert / Flee`（低血量触发逃离，失去视野可保持短时警戒）
- [x] 噪音事件接入：玩家动作生成噪音，怪物可在无视野时被噪音吸引进入 Alert
- [x] AI 回归测试新增 2 项：噪音追踪与低血量逃离

## 进行中（In Progress）
- [ ] 更完整的物品体系（更多消耗品/任务道具、装备词条扩展；当前已完成通用物品栈 + 3 槽位装备 MVP）

## 待实现（Todo）
- [ ] 支线合约系统（kill/collect/time_limit/stealth）
- [ ] 环境交互（门/锁/陷阱/噪音传播）
- [ ] 远程攻击与技能系统
- [ ] 更完善日志与调试开关（`RUST_LOG`）
- [ ] 更多自动化测试（FOV、AI 决策、回归测试）

## 当前可用命令
- 运行：`cargo run -- --seed 123`
- 测试：`cargo test`
- 构建：`cargo build`

## 本次验证记录
- `cargo test`：23/23 通过
- `cargo build`：通过
- `cargo fmt`：通过

## 下次续开发建议入口
1. 扩展物品体系（更多消耗品/任务道具、装备词条与掉落分层）。
2. 进入支线合约系统与奖励结算，推进 M4。
3. 继续完善 AI（状态可视化日志、噪音衰减参数化）。
